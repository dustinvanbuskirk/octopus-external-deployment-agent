# Changelog

All notable changes to this project will be documented here.

---

## [Unreleased]

### Added
- Initial release of `register_k8s_agent.py`
- Dual-mode config: INI file (local) and Octopus environment variables (runbook)
- `config.example.ini` template for per-space configurations
- `requirements.txt` confirming stdlib-only dependencies
- `.gitignore` protecting populated config files from accidental commit
- Full `README.md` documenting all five execution options